<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
  <!-- Menu-bar app: no Dock icon, no ⌘-Tab entry — from the very first frame,
       so there's no Dock flash on launch. (In `tauri dev` the bundle plist
       isn't used; the runtime Accessory policy covers dev.) -->
  <key>LSUIElement</key>
  <true/>
</dict>
</plist>
